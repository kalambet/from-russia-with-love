---
# Shared defaults — no provider-specific config here
credentials_output_dir: "{{ playbook_dir }}/../credentials"
op_vault: "Infrastructure"

# 1Password path to SSH public key
# Example: "op://Infrastructure/SSH Key/public key"
op_ssh_key_item: "op://Infrastructure/ssh-key/public key"
